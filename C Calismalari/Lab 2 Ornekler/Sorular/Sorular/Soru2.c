/*
 #include <stdio.h>

int main(){
    
    double shopCost,total;
    printf("Enter the shop cost\n");
    scanf("%lf",&shopCost);
    
    if(shopCost<200){
        total = (shopCost - (shopCost*0.1));
        printf("Total Cost = %2.2f ₺'dir\n",total);
    }
    else if(shopCost>=200 && shopCost <=400){
        total = (shopCost - (shopCost*0.15));
        printf("Total Cost = %2.2f ₺'dir\n",total);
    }else if(shopCost > 400){
        total = (shopCost - (shopCost*0.20));
        printf("Total Cost = %2.2f ₺'dir\n",total);
    }
    
}
*/
